req:
  #各子模块的增查改删
  - crud:
      1、增：用户创建私有空间
      2、查：管理员搜索空间
      3、改：管理员编辑空间
      4、删：管理员删除空间
plan:
  # 工作流程
  - workflow:
      1、公共图库与私有空间完全独立设计，一个用户可以创建最多一个私有空间
      2、用户只能访问和管理自己的空间，用户私有空间的图片不会展示在公共图库，也不需要管理员审核
      3、不同空间级别（比如普通版、专业版），对应了不同的容量和图片的数量限制
    # 接口设计
  - interface:
    # 库表设计
  - database:
dev:
  # exceptions、enums、aop、config、constants、utils、helpers
  - base:
    # dto（request）、bo（result）、po（entity）、vo
  - model:
    # mapper、service、controller
  - biz: