req:
  #各子模块的增查改删
  - crud:
      1、查询优化：分布式缓存、本地缓存、多级缓存
      - 1、key：业务:方法:${查询条件key}）
      - 2、value：考虑可读性可存JSON，考虑压缩可存二进制
      - 3、ttl：5~60分钟
      2、上传优化：压缩、分片、续传、秒传
      - 1、转换图片格式（WebP）
      - 2、压缩图片、缩小尺寸
      3、加载优化：CDN、缩略图、懒加载、浏览器缓存
      4、存储优化：数据沉降、冷热分离、清理策略
      5、其他拓展优化：统一管理缓存、手动刷新缓存、解决缓存常见问题、自动识别热点缓存、查询必要字段
plan:
  # 工作流程
  - workflow:
    # 接口设计
  - interface:
    # 库表设计
  - database:
dev:
  # exceptions、enums、aop、config、constants、utils、helpers
  - base:
    # dto（request）、bo（result）、po（entity）、vo
  - model:
    # mapper、service、controller
  - biz: